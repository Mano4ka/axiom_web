# Axiom Web 🌐

Личный проект на Next.js - лендинг. Проект был разработан по запросу, но дальнейшая разработка остановлена. Оставляю здесь как пример своего кода.

## 🚀 Запуск для разработки

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 🌍 Запуск на сервере (production)

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Соберите проект:
   ```bash
   npm run build
   ```

3. Запустите production-сервер:
   ```bash
   npm start
   ```

Приложение будет доступно на порту 3000.

## 📦 Статический экспорт

Проект также поддерживает статический экспорт для хостинга на статических серверах (например, GitHub Pages, Netlify).

После билда (`npm run build`) статические файлы генерируются в папке `out/`:
- `index.html` - главная страница
- `404.html` - страница ошибки
- `_next/` - скомпилированные JS и CSS файлы
- `images/`, `fonts/`, `icons/` - статические ресурсы

Эти файлы можно загрузить на любой статический хостинг.

## 📁 Структура проекта

- **`app/`** - Основные страницы и layout Next.js App Router
- **`components/`** - Переиспользуемые компоненты React (Hero, Header, Footer и т.д.)
- **`data/`** - JSON-файлы с данными (FAQ, footer, header и т.д.)
- **`lib/`** - Утилиты и хуки (i18n, анимации)
- **`public/`** - Статические файлы (изображения, шрифты, иконки)

## 🛠 Технологии

- **Next.js** - React фреймворк
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - CSS фреймворк
- **ESLint** - Линтер кода

---

*Проект опубликован для демонстрации.*
